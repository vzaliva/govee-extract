# Secrets -- never commit an API key
api_key
*.api_key
.env
influx_token
secrets.*

# Local agent/editor config
.claude/
CLAUDE.md

# Python / uv
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
# uv.lock is committed deliberately -- this is an application, not a library

# Local data captured from the device
*.csv
*.log
